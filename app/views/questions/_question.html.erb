<tr>
  <td><%= @question.body %></td>
  <td><%= @question.test.title %></td>
  <td><%= link_to 'Show', question_path(question) %></td>
  <td><%= link_to 'Edit', edit_question_path(question) %></td>
  <td><%= link_to "Delete", [question], data: {
            turbo_method: :delete,
            turbo_confirm: "Are you sure?"
              } %>
</tr>
